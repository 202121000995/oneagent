# Kernel binaries

Put Linux kernel release files here before building an offline package.

Accepted examples:

- `sing-box-1.13.13-linux-amd64.tar.gz`

`deploy/package-offline.sh` will extract this file and include `sing-box` in the offline zip.
